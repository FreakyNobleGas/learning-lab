# No API key needed when using Ollama locally.
# ADK still requires these variables to be set — FALSE tells it not to use Google's cloud.
GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_GENAI_API_KEY=no-key-needed
